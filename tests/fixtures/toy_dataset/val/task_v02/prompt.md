What is the canonical deploy script path? Reply with exactly one line in this format:
canonical deploy script is <path>

Use the harness if it helps. Do not guess. If the harness does not contain the answer, reply exactly:
I don't know deploy script
